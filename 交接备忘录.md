# 开发交接备忘录

> 最近更新：2026-07-18
> 对应分支：`main`
> 本轮主题：生产化加固（数据安全/归属/演示模式/汇总）+ 对比讲解视频导出（ffmpeg 全链路，⛔ 已封存停用，见下节）

---

## 〇、视频导出功能（⛔ 已封存，2026-07-18 起注释停用，勿再继续开发）

> **⛔ 状态：已注释停用（用户决定）。** 入口按钮已在 `src/components/ComparePlayer.jsx` 注释掉（第 7 行 import + 第 804-817 行 JSX 块，注释原文「视频导出功能暂时注释，使用屏幕录制替代」）。日常演示改用**屏幕录制**替代。
> **不要再继续开发/修复/重启这个功能**，除非用户明确提出恢复。相关代码全部保留未删（`electron/videoExport.js`、`electron/subtitleAss.js`、`src/utils/exportCanvas.js`、`src/components/ExportVideoButton.jsx`，main.js 的 IPC 仍注册着），如需恢复只需取消 ComparePlayer.jsx 中那两处注释即可。
> 下方内容仅作历史存档，供将来恢复时参考。

**架构**：渲染进程备料（标注PNG/TTS音频/字幕timing）→ 主进程 `electron/videoExport.js` 用 `ffmpeg-static` 合成 MP4。入口在对比播放器头部「🎞 导出视频」按钮（演示模式下隐藏）。

| 文件 | 职责 |
|---|---|
| `electron/videoExport.js` | filter_complex 构图（裁剪→统一帧率→拼接→标注overlay→字幕→配音混流）、进度上报 |
| `electron/subtitleAss.js` | timing map → ASS 字幕（复用播放器同一 `generateTimingMap`，口径一致） |
| `src/utils/exportCanvas.js` | 标注→透明PNG：布局取整复刻 ffmpeg `-2` 就近取偶（已实测 405→406、404.64→404），绘制复刻 AnnotationLayer SVG 语义 |
| `src/components/ExportVideoButton.jsx` | 数据流串联：探测分辨率/生成配音/画标注/调用导出 |

**已实测锁定的坑**（都写了注释，改动前先看）：
- 前后视频帧率不一致时 stack 滤镜帧数爆炸卡死 → 拼接前必须 `fps=30` 统一
- 短侧视频用 `tpad` 定格补齐（这是「改善后更快」的直观证明，是特性不是bug）
- ffmpeg-static 普通二进制不会被 electron-builder 自动解包 → package.json 已配 `asarUnpack`
- 本机 conda 的 ffmpeg 无 libass，测字幕必须用 `node_modules/ffmpeg-static/ffmpeg`
- `drawbox` 在透明底上画的东西 alpha=0（测试素材坑，canvas 产出的 PNG 无此问题）

**已收尾（2026-07-18）**：
- ✅ 中文字体已内置：`electron/assets/fonts/NotoSansCJKsc-Regular.otf`（OFL 许可，约16MB），族名 "Noto Sans CJK SC"（fc-scan 实测），asarUnpack 已配 `electron/assets/**`；已验证仅用内置目录即可烧录中文字幕
- ✅ 全局模式导出：所有工序逐段导出→concat 无重编码拼接；无配音段用 anullsrc 补静音轨保证各段流结构一致（否则 -c copy 拼接失败），已实测两段拼接（有配音+无配音）时长/字幕/流结构正确
- ✅ 导出取消：导出中再点按钮即取消（主进程 SIGKILL ffmpeg + 清理临时文件）
- ✅ 数据恢复入口：项目列表页「🛟 数据恢复」，列启动备份快照，恢复前自动另存当前库（prerestore_*.db），恢复后应用自动重启

**待办**（⛔ 随功能一并封存，不要继续做）：
- ~~未用真实工序视频+真实TTS在 Electron GUI 里跑过~~ —— 功能已停用，不再需要
- ~~Windows 打包分发验证（含 ffmpeg/字体 asarUnpack 路径、代码签名决策）~~ —— 功能已停用，不再需要

---

## 一、本轮已完成（3 个 Sprint，全部通过验证）

### ✅ 大屏轮播 & 全屏播放（2026-07-18，本轮新增）

**1) 大屏自动切换卡死修复 → 判定重写**（`src/components/ComparePlayer.jsx`）
- 根因：大屏只播当前 tab 的视频，旧逻辑走「双视频同时播完」判定，改善后视频永远暂停在起点 → 卡死在改善前；且阶段判断依赖 `activeTab`（异步 state，事件回调里是旧值），判定散在 timeupdate/ended 两处，事件丢失即整体卡死。
- 重写后架构（改动前先看懂再动）：
  - `fsPhaseRef`：大屏阶段的**同步事实来源**，所有写点（handlePlay 重启 / switchFullscreenToAfter / handleTabClick / 工序重置 effect / 分离模式切轨）同步更新；`activeTab` 只管 UI 显示，判定一律读 ref。
  - `evaluateFullscreen()`：**唯一判定入口**，timeupdate / ended / 看门狗三处共用，整体 try/catch 防静默断链；分离配音（AI开）仍走既有音轨状态机，函数内直接 return。
  - **看门狗**：大屏播放中每 250ms 强制判定一次（经 `evaluateFullscreenRef` 调最新闭包，interval 不随渲染重建），timeupdate/ended 全丢也不会卡死。
  - 重入守卫：`switchFullscreenToAfter` 靠 `afterVideoRef.paused` 防重复切换；`finishCurrentProcess` 靠 `finishGuardRef` 防双入口连跳两个工序（handlePlay 开始新段时复位）。**两个守卫都不能删。**
- new_step 工序直接从改善后阶段开始；cancelled 工序改善前放完即完成；配音未讲完则视频循环等待。

**2) 大屏自适应布局**（`src/App.jsx` + `src/styles/App.css`）
- 大屏模式 `.video-container` 加 `fullscreen-fit`：根因是 flex 子项 `min-height:auto` 被内容撑开导致页面滚动，全链路 `min-height:0` + 视频区 `flex:1`，画面吃满剩余高度。
- 隐藏标题行/视频小标题/改善说明；统计块收纳为「📊 统计」按钮触发的绝对定位浮层（不占布局）；大屏视频 `object-fit: contain`（标注坐标换算本就按 contain 算）。

**3) 视频全屏（video-fullscreen）UI 修复**（App.css 末尾规则块）
- 底部控制条按钮 `white-space: nowrap`（"从头开始"曾折行）；时长胶囊深色半透明+右边距（原贴屏幕右缘被裁）；全屏隐藏标注编辑按钮。

**⚠️ 待 GUI 实测清单**（单测/build 已过，播放时序仓库惯例必须实跑）：大屏 AI开/关 × 整合/分离配音、new_step/cancelled 工序、连续播放勾选、改善前→改善后→下一工序自动流转、看门狗兜底（播到文件尾）、统计浮层、视频全屏 Esc 退出。

### ✅ Sprint 0 — 数据安全兜底
文件：`electron/database.js`

| 修复 | 说明 |
|---|---|
| **外键级联失效** | SQLite 默认不开外键，better-sqlite3 也不自动开 → 之前所有 `ON DELETE CASCADE` 都没生效，删项目会留下孤儿 stages/processes/annotations。已在连接打开后 `pragma('foreign_keys = ON')`。 |
| **孤儿数据清理** | 新增 `cleanupOrphans()`，启动时自上而下清理历史脏数据（事务内，子表靠级联自动清）。 |
| **数据库备份** | 新增 `backupDatabase()`，每次启动前把 `improvement.db` 滚动备份到 `userData/db_backups/`，保留最近 5 份。 |

**验证方式**：在真实 Electron 运行时用临时库实跑「插入项目→阶段→工序→标注 → 删项目 → 查残留」，确认 `foreign_keys=1` 且删除后三张子表全部归零；多次启动确认备份份数正确。

### ✅ Sprint 1 — 工程化地基
新增：`eslint.config.js`、`.prettierrc.json`、`.prettierignore`、`vitest.config.js`

- **Vitest** 测试框架，覆盖 `utils/`（time / timing / annotation / narration / export-CSV转义）共 **47 个测试**。
- **ESLint**（flat config，分主进程 CJS / 配置 ESM / 渲染 React 三套规则）。基线 **0 error**，剩 ~68 个 warning（多为 `exhaustive-deps`，见下方待办）。
- **Prettier** 统一格式。
- `package.json` 新增脚本：`test` / `test:watch` / `lint` / `format`。

**顺手修复 2 个真·崩溃 bug**（ESLint `rules-of-hooks` 抓出）：
1. `src/components/ProcessTimelineMarker.jsx` — `useMemo` 写在提前 `return null` 之后，工序列表从有变无时会触发 React "rendered fewer hooks" 白屏。已把 Hook 提到 return 之前。
2. `src/components/ComparePlayer.jsx` — 字幕 `useMemo` 内联在 JSX 里、位于 `if(!currentProc) return` 之后，切换/清空工序时崩溃。已上提为顶层 `subtitleText`。

### ✅ Sprint 2（第一阶段）— 播放纯逻辑抽离
新增：`src/utils/narration.js`（+ 13 个测试）

从 1000 行的 `ComparePlayer` 双音轨状态机里抽出 4 个纯函数并用测试锁定，再在原处做 **1:1 行为不变替换**：

| 纯函数 | 替换掉的脆弱点 |
|---|---|
| `buildNarrationPlaylist` | 「索引 [0]=改善前 [1]=改善后 固定结构、空文本不过滤」不变量（历史 bug 根源） |
| `computeProgress` | 重复 3 处的进度钳制计算 |
| `isAudioEnded` | 重复 2 处的「音频结束 + 0.2s 容差」判定 |
| `shouldSwitchPhase` | 阶段切换判定（立即切换 vs 视频音频都完成） |

**验证方式**：47 单测全绿 + `vite build` 通过 + 浏览器 preview 冒烟（整条模块图加载、UI 渲染无回归；唯一报错是浏览器无 `electronAPI`，与改动无关）。

### ✅ Sprint 3 — 健壮性
文件：`electron/database.js`、`electron/main.js`

| 修复 | 说明 |
|---|---|
| **迁移机制加固** | 6 处「静默吞错的 `try{ALTER}catch{}`」改为 `addColumnIfMissing()`：先查 `PRAGMA table_info` 再加列，只跳过「列已存在」，真实错误不再被吞。加 `user_version` 版本戳（当前基线 v1）为将来复杂迁移铺路。 |
| **协议路径加固** | `local-video://` 处理器加防御纵深：拒空字节、拒非绝对路径、校验文件真实存在且为常规文件，拒绝时返回错误码并记录。注意只拒空字节、**不拒空格**（合法路径含空格）。 |
| **导出文件名防碰撞** | 导出时给每个复制文件加单调递增序号 `fileSeq`，避免同毫秒 `Date.now()` 命名碰撞。 |

**验证方式**：Electron 运行时实测迁移三场景全过——全新库（列齐全、user_version=1）/ 模拟旧库（缺列自动补齐、旧数据保留、版本戳更新）/ 重复打开幂等无报错。

---

## 二、常用命令

```bash
npm run dev      # 同时起 Vite + Electron（完整应用）
npm run dev:react# 只起 Vite（端口 8547，浏览器预览用，无 electronAPI）
npm test         # 跑全部单测（vitest run）
npm run lint     # ESLint（应保持 0 error）
npm run format   # Prettier 格式化
npm run build    # 生产构建
npm run dist     # 打包（electron-builder）
```

> `.claude/launch.json` 已配好 `kaizen-react`，可直接用 Claude 的 preview 工具做浏览器冒烟（注意：浏览器里 `window.electronAPI` 不存在，DB/TTS/视频功能用不了，只能验渲染与模块加载）。

---

## 三、下一步待办（按优先级）

### 🔴 Sprint 2（第二阶段）— 深度结构重构 ⚠️ 需在 Electron GUI 实测
- 把 `ComparePlayer`（1000+ 行）真正拆成 `useDualAudio` / `useNarrationClock` / `useSubtitleSync` 自定义 hook，多文件化。
- 引入 Context（`PlaybackContext` / `ProjectContext`），消除 `App.jsx` 的 props 透传与「语速回弹」补丁（现靠 `window.dispatchEvent('project-updated')` 打补丁）。
- **必须配真实视频在 Electron 里手动验证**播放/阶段切换/字幕同步——单测和浏览器预览都到不了这层运行时，改完无法自动确认无回归。

### 🟡 收尾清理
- ✅ ~~3 个纯加载 effect 的 exhaustive-deps~~（已完成：`ProjectList`/`StageManager`/`App` 用 `useCallback` 包裹，已实测无无限重渲染）。
- ⏳ 剩 ~58 个 ESLint warning，重点是 `VideoPlayer`/`ComparePlayer`/`AnnotationLayer` 的 `react-hooks/exhaustive-deps`（与播放时序强耦合，**必须配 GUI 逐个判断**，盲修易引入循环/重渲染回归）。其余多为无害的 unused-vars。
- ✅ ~~主进程 console.log 包 dev 开关~~（已完成：`dlog()`）。
- ✅ ~~TTS 语速换算注释与代码不符~~（已完成：注释已校正）。

---

## 四、踩坑提醒
- **`better-sqlite3` 是为 Electron 编译的**，不能用系统 `node` 直接 require（NODE_MODULE_VERSION 不匹配）。要在 Electron 运行时里测 DB，用 `./node_modules/.bin/electron <脚本>`。
- 改 `ComparePlayer` 的播放/切换逻辑后，单测和 build 通过 **不等于** 行为正确——务必 GUI 实跑。
- 历史经验（见 `开发总结与失败经验_20251225.md`）：字幕切换的竞态/残留靠 `key` 强制重绘解决；逻辑改烂时及时 `git` 回滚，别在烂泥上盖楼。
