# AI 讲解同步播放开发阶段总结 (2025-12-25)

## 🏁 今日完成进度 (Progress)

今天的主要目标是完善 **「前后分离模式」** 的 AI 讲解同步逻辑，确保在这一复杂模式下，视频、音频、字幕能达到“单人讲解”的流畅感。

1.  **自动相位切换逻辑**：
    *   在 `ComparePlayer.jsx` 中重写了 `handleTimeUpdate` 函数。
    *   实现了当「改善前」视频+音频播放完成后，系统自动切换音轨、更新字幕、重置 `activeTab` 并启动「改善后」阶段。
2.  **播放列表稳定性**：
    *   优化了 `loadTTS` 逻辑，确保 Playlist 结构固定，使索引 `[0]` 始终对应改善前，`[1]` 始终对应改善后，即便文字为空也不再过滤掉 Track。
3.  **UI 选项卡同步**：
    *   实现了 `activeTab` 随着讲解进度的自动跳转，并在手动点击标签时增加了强制清理旧播放状态的逻辑。
4.  **字幕组件强制重拉 (Key Reset)**：
    *   给 `SubtitleOverlay` 添加了组合 Key，确保在切换阶段时彻底销毁并重建组件，以解决旧字幕残留显示的 Bug。

---

## ❌ 失败经验总结与教训 (Failure Experience Summary)

在完成上述功能的过程中，我们遇到了一些逻辑死循环和视觉故障，核心教训如下：

### 1. 状态切换的时序错位 (Race Conditions)
*   **现象**：切换到改善后阶段的一瞬间，字幕显示的仍是改善前最后一句。
*   **原因**：React 的批量更新机制导致 `activeTab` 变了，但 `timingData` 还是旧引用，而 `SubtitleOverlay` 的渲染频率极高（依赖播放时间），在切换的几毫秒内抓到了错误的数据。
*   **反思**：对于这种高频刷新且强依赖数据的组件，**“强制卸载重启” (使用 Key)** 比“试图更新 Props”更可靠。

### 2. 视觉隔离 (`video-hidden`) 引发的事件中断
*   **现象**：使用 `display: none` 隐藏视频后，发现自动切换逻辑偶尔失效。
*   **原因**：部分浏览器（特别是 Electron 内核）会为了节省资源，在元素隐藏时降低或停止触发 `onTimeUpdate` 事件。
*   **教训**：控制逻辑（特别是切换阶段的判断）不应依赖可能会被 UI 隐藏的组件事件流，而应由全局唯一的 Audio/Clock 驱动。

### 3. Git 操作与逻辑回退
*   **现象**：在多次重试解决字幕残留时，代码逻辑变得异常臃肿且出现死循环。
*   **教训**：当逻辑分支多到大括号对齐都困难时，**“及时止损、通过 Git 回滚到稳定版本”** 是明智的，而不是在烂泥上盖楼。今天通过 `8871601` 版本的快速回切，保护了核心功能的可用性。

### 4. 运行环境干扰
*   **现象**：`npm run dev` 频繁报错。
*   **原因**：本地端口占用的惯性问题。在开发这种强依赖 Electron/Node 通信的功能时，稳定的运行环境是排障的前提，否则容易把环境问题误判为代码邏輯问题。

---

## 📅 明日（后续）重点
1.  **Seek 逻辑适配**：由于引入了双音轨，用户拖动主进度条后半段时的“跨区”跳转逻辑还需进一步加固。
2.  **代码解耦**：将 `handleTimeUpdate` 内部的分支逻辑提取到外部工具函数中。
